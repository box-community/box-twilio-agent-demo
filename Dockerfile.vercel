FROM node:22-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV HOST=0.0.0.0

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY tsconfig.json ./
COPY data ./data
COPY lib ./lib
COPY src/voice ./src/voice

CMD ["npm", "run", "voice"]
